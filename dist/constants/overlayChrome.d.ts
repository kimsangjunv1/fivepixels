/** Idle time before expanded panel/pin auto-collapse to a peek. */
export declare const OVERLAY_IDLE_COLLAPSE_MS = 12000;
/** Delay after host-page pointer activity before chrome yields (dims). */
export declare const OVERLAY_HOST_YIELD_DELAY_MS = 280;
/** How long yield stays after the last host interaction. */
export declare const OVERLAY_HOST_YIELD_HOLD_MS = 1600;
export declare const PIN_RAIL_BUBBLE_SIZE = 52;
export declare const PIN_RAIL_EXPANDED_WIDTH = 280;
export declare const OVERLAY_EDGE_MARGIN = 16;
/** Fraction of the collapsed bubble that sits off-screen while peeking. */
export declare const PIN_PEEK_HIDDEN_RATIO = 0.42;
export declare const FIVEPIXELS_HOST_ID = "fivepixels-root";
//# sourceMappingURL=overlayChrome.d.ts.map