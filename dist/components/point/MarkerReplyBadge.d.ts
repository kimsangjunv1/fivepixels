type MarkerReplyBadgeProps = {
    size: number;
    /** Marker fill color — badge background matches the marker. */
    accentColor: string;
};
/** Satellite badge on the top-right of a marker when the thread has replies. */
export declare function MarkerReplyBadge({ size, accentColor }: MarkerReplyBadgeProps): import("react").JSX.Element;
export {};
//# sourceMappingURL=MarkerReplyBadge.d.ts.map